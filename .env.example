# Optional legacy Discogs token (global). Prefer setting per-user in Account.
DISCOGS_TOKEN=

# Public URL used in activation emails (e.g. https://vinylcat.example.com)
PUBLIC_BASE_URL=

# SMTP settings (required for email activation in hosted mode)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=VinylCat <no-reply@example.com>
SMTP_USE_TLS=true
SMTP_USE_SSL=false
